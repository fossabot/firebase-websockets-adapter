{
  "name": "firebase-websockets-adapter",
  "version": "1.0.18",
  "description": "A Firebase Websockets adapter",
  "main": "dist",
  "scripts": {
    "dev": "nodemon -w src --exec \"babel-node src --presets es2015,stage-0\"",
    "build": "babel src -s -D -d dist --presets es2015,stage-0",
    "start": "node dist",
    "prestart": "npm run -s build",
    "lint": "eslint src",
    "prepublish": "npm-auto-version",
    "test": "echo 'test script is not defined'"
  },
  "author": "Matthew Wozniak <matisiek11@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-core": "^6.9.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0",
    "eslint": "^3.1.1",
    "nodemon": "^1.9.2"
  },
  "dependencies": {
    "jwt-simple": "^0.5.1",
    "ws": "^5.2.1"
  }
}